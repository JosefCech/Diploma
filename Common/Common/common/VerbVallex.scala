package Vallex

/*
class Verb(val lemma : String , frames : ValencyFrame) {
  
  def LemmaForm = {
    if (lemma.contains("_")) {
     val index = lemma.indexOf("_")
     lemma.substring(0, index);
    }
    else {
      lemma
    }
  }
  
}*/